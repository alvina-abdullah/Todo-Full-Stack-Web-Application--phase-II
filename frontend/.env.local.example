# Frontend Environment Variables

# Better Auth secret for JWT token signing and verification
# IMPORTANT: Generate a secure random string using: openssl rand -base64 32
# This MUST match the BETTER_AUTH_SECRET in backend/.env
BETTER_AUTH_SECRET='your-secret-key-here-generate-with-openssl-rand-base64-32'

# Database connection string for Better Auth (Neon PostgreSQL)
# Format: postgresql://user:password@host/database?sslmode=require
# Note: Use postgresql:// (not postgresql+asyncpg://) for Better Auth
DATABASE_URL='postgresql://your_user:your_password@your_host.neon.tech/your_database?sslmode=require'

# Backend API URL
# Development: http://localhost:8001
# Production: https://your-backend-domain.com
NEXT_PUBLIC_API_URL=http://localhost:8001
